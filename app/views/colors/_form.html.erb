<%= form_with(model: color) do |form| %>
  <% if color.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(color.errors.count, "error") %> prohibited this color from being saved:</h2>

      <ul>
        <% color.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :min, style: "display: block" %>
    <%= form.number_field :min %>
  </div>

  <div>
    <%= form.label :max, style: "display: block" %>
    <%= form.number_field :max %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.number_field :price %>
  </div>

  <div>
    <%= form.collection_check_boxes :finishing_ids, Finishing.all, :id, :title, {}, {:multiple => true } %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
